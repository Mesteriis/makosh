[package]
name = "makosh-tasks-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-tasks-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "domain"
owner = "tasks"
surface = "assembly"

[dependencies]
makosh-tasks-persistence = { path = "../tasks-persistence" }
makosh-tasks-runtime = { path = "../tasks-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
