[package]
name = "makosh-search-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "projection"
owner = "search"
surface = "core"

[dependencies]
sha2 = { version = "=0.11.0", default-features = false }
