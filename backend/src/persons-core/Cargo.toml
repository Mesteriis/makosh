[package]
name = "makosh-persons-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "domain"
owner = "persons"
surface = "implementation"

[dependencies]
makosh-persons-api = { path = "../persons-api" }
sha2 = { version = "=0.11.0", default-features = false }
