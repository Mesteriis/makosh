[package]
name = "makosh-communication-recipient-suggestion-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-recipient-suggestion-runtime"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_recipient_suggestion"
surface = "runtime"

[dependencies]
makosh-blob-client = { path = "../platform/blob/client" }
makosh-communication-recipient-suggestion-api = { path = "../communication-recipient-suggestion-api" }
makosh-communication-recipient-suggestion-core = { path = "../communication-recipient-suggestion-core" }
makosh-communication-recipient-suggestion-persistence = { path = "../communication-recipient-suggestion-persistence" }
makosh-communications-recipient-source-api = { path = "../communications-recipient-source-api" }
makosh-events-jetstream = { path = "../platform/events/jetstream" }
makosh-events-protocol = { path = "../platform/events" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
makosh-storage-vault = { path = "../platform/storage/vault" }
libc = "=0.2.186"
prost = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }
tokio = { version = "=1.52.4", default-features = false, features = ["rt-multi-thread", "time"] }
zeroize = "=1.9.0"
