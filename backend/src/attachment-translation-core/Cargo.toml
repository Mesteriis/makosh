[package]
name = "makosh-attachment-translation-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "workflow"
owner = "attachment_translation"
surface = "implementation"

[dependencies]
