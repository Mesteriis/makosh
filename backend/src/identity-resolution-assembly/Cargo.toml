[package]
name = "makosh-identity-resolution-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-identity-resolution-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "engine"
owner = "identity_resolution"
surface = "assembly"

[dependencies]
makosh-identity-resolution-persistence = { path = "../identity-resolution-persistence" }
makosh-identity-resolution-runtime = { path = "../identity-resolution-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
