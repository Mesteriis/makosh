[package]
name = "makosh-mail-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-mail-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "integration"
owner = "mail"
surface = "assembly"

[dependencies]
makosh-mail-persistence = { path = "../mail-persistence" }
makosh-mail-runtime = { path = "../mail-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
