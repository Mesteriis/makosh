[package]
name = "makosh-communication-delivery-intent-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-delivery-intent-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_delivery_intent"
surface = "assembly"

[dependencies]
makosh-communication-delivery-intent-persistence = { path = "../communication-delivery-intent-persistence" }
makosh-communication-delivery-intent-runtime = { path = "../communication-delivery-intent-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
