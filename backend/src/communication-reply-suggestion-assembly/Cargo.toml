[package]
name = "makosh-communication-reply-suggestion-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-reply-suggestion-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_reply_suggestion"
surface = "assembly"

[dependencies]
makosh-communication-reply-suggestion-persistence = { path = "../communication-reply-suggestion-persistence" }
makosh-communication-reply-suggestion-runtime = { path = "../communication-reply-suggestion-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
