[package]
name = "makosh-speech-to-text-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "engine"
owner = "speech_to_text"
surface = "implementation"

[dependencies]
