[package]
name = "makosh-whatsapp-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "integration"
owner = "whatsapp"
surface = "implementation"

[dependencies]
makosh-communications-ingress = { path = "../communications-ingress" }
makosh-whatsapp-api = { path = "../whatsapp-api" }
