[package]
name = "makosh-communication-note-candidate-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-note-candidate-runtime"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_note_candidate_extraction"
surface = "runtime"

[dependencies]
makosh-blob-client = { path = "../platform/blob/client" }
makosh-communication-note-candidate-api = { path = "../communication-note-candidate-api" }
makosh-communication-note-candidate-core = { path = "../communication-note-candidate-core" }
makosh-communication-note-candidate-persistence = { path = "../communication-note-candidate-persistence" }
makosh-communications-note-source-api = { path = "../communications-note-source-api" }
makosh-events-jetstream = { path = "../platform/events/jetstream" }
makosh-events-protocol = { path = "../platform/events" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-review-note-candidate-api = { path = "../review-note-candidate-api" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
makosh-storage-vault = { path = "../platform/storage/vault" }
libc = "=0.2.186"
prost = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }
tokio = { version = "=1.52.4", default-features = false, features = ["rt-multi-thread", "time"] }
zeroize = "=1.9.0"
