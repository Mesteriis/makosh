[package]
name = "makosh-communication-bulk-action-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-bulk-action-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_bulk_action"
surface = "assembly"

[dependencies]
makosh-communication-bulk-action-persistence = { path = "../communication-bulk-action-persistence" }
makosh-communication-bulk-action-runtime = { path = "../communication-bulk-action-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
