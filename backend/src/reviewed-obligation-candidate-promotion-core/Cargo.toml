[package]
name = "makosh-reviewed-obligation-candidate-promotion-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "workflow"
owner = "reviewed_obligation_candidate_promotion"
surface = "implementation"

[dependencies]
sha2 = { version = "=0.11.0", default-features = false }
