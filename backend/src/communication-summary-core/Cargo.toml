[package]
name = "makosh-communication-summary-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_summary"
surface = "implementation"

[dependencies]
