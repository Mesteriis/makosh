[package]
name = "makosh-communication-note-candidate-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-note-candidate-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_note_candidate_extraction"
surface = "assembly"

[dependencies]
makosh-communication-note-candidate-persistence = { path = "../communication-note-candidate-persistence" }
makosh-communication-note-candidate-runtime = { path = "../communication-note-candidate-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
