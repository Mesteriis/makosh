[package]
name = "makosh-attachment-preview-renderer-contract"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "workflow"
owner = "attachment_preview"
surface = "contract"

[dependencies]
makosh-attachment-preview-api = { path = "../attachment-preview-api" }
