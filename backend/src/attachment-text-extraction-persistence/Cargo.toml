[package]
name = "makosh-attachment-text-extraction-persistence"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "workflow"
owner = "attachment_text_extraction"
surface = "persistence"

[dependencies]
makosh-attachment-text-extraction-core = { path = "../attachment-text-extraction-core" }
makosh-attachment-text-extraction-ingress = { path = "../attachment-text-extraction-ingress" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
sha2 = { version = "=0.11.0", default-features = false }
sqlx = { version = "=0.9.0", default-features = false, features = ["postgres", "runtime-tokio", "tls-rustls-ring"] }
