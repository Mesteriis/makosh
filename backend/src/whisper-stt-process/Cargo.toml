[package]
name = "makosh-whisper-stt-process"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "integration"
owner = "whisper_stt"
surface = "implementation"

[dependencies]
makosh-speech-to-text-api = { path = "../speech-to-text-api" }
makosh-whisper-stt-core = { path = "../whisper-stt-core" }
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
