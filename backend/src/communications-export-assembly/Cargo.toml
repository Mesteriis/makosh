[package]
name = "makosh-communications-export-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communications-export-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communications_export"
surface = "assembly"

[dependencies]
makosh-communications-export-persistence = { path = "../communications-export-persistence" }
makosh-communications-export-runtime = { path = "../communications-export-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
