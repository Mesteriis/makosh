[package]
name = "makosh-zulip-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-zulip-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "integration"
owner = "zulip"
surface = "assembly"

[dependencies]
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
makosh-zulip-persistence = { path = "../zulip-persistence" }
makosh-zulip-runtime = { path = "../zulip-runtime" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
