[package]
name = "makosh-ai-inference-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "engine"
owner = "ai"
surface = "implementation"

[dependencies]
makosh-ai-contracts = { path = "../ai-contracts" }
sha2 = { version = "=0.11.0", default-features = false }
