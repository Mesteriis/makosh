[package]
name = "makosh-communication-delivery-intent-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-delivery-intent-runtime"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_delivery_intent"
surface = "runtime"

[dependencies]
makosh-blob-client = { path = "../platform/blob/client" }
makosh-communication-delivery-intent-api = { path = "../communication-delivery-intent-api" }
makosh-communication-delivery-intent-core = { path = "../communication-delivery-intent-core" }
makosh-communication-delivery-intent-event-adapters = { path = "../communication-delivery-intent-event-adapters" }
makosh-communication-delivery-intent-ingress-api = { path = "../communication-delivery-intent-ingress-api" }
makosh-communication-delivery-intent-persistence = { path = "../communication-delivery-intent-persistence" }
makosh-communications-api = { path = "../communications-api" }
makosh-events-jetstream = { path = "../platform/events/jetstream" }
makosh-events-protocol = { path = "../platform/events" }
makosh-mail-delivery-intent-contract = { path = "../mail-delivery-intent-contract" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
makosh-storage-vault = { path = "../platform/storage/vault" }
makosh-telegram-delivery-intent-contract = { path = "../telegram-delivery-intent-contract" }
makosh-whatsapp-delivery-intent-contract = { path = "../whatsapp-delivery-intent-contract" }
makosh-zulip-delivery-intent-contract = { path = "../zulip-delivery-intent-contract" }
libc = "=0.2.186"
prost = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }
tokio = { version = "=1.52.4", default-features = false, features = ["rt", "rt-multi-thread", "time"] }
zeroize = "=1.9.0"
