[package]
name = "makosh-whisper-stt-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "integration"
owner = "whisper_stt"
surface = "implementation"

[dependencies]
makosh-speech-to-text-api = { path = "../speech-to-text-api" }
makosh-speech-transcript-artifact = { path = "../speech-transcript-artifact" }
