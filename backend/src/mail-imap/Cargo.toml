[package]
name = "makosh-mail-imap"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[features]
default = []
conformance-test-support = []

[package.metadata.makosh]
role = "integration"
owner = "mail"
surface = "implementation"

[dependencies]
async-imap = "=0.11.2"
async-native-tls = "=0.6.0"
async-std = "=1.13.2"
futures-util = "=0.3.32"
makosh-mail-core = { path = "../mail-core" }
makosh-mail-api = { path = "../mail-api" }
imap-proto = "=0.16.7"
