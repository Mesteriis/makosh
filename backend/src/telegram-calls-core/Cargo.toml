[package]
name = "makosh-telegram-calls-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "integration"
owner = "telegram"
surface = "implementation"

[dependencies]
makosh-scheduler-protocol = { path = "../platform/scheduler/protocol" }
sha2 = { version = "=0.10.9", default-features = false }
