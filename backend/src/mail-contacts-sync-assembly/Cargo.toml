[package]
name = "makosh-mail-contacts-sync-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-mail-contacts-sync-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "mail_contacts_sync"
surface = "assembly"

[dependencies]
makosh-mail-contacts-sync-persistence = { path = "../mail-contacts-sync-persistence" }
makosh-mail-contacts-sync-runtime = { path = "../mail-contacts-sync-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
