[package]
name = "makosh-communication-delayed-delivery-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-communication-delayed-delivery-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_delayed_delivery"
surface = "assembly"

[dependencies]
makosh-communication-delayed-delivery-persistence = { path = "../communication-delayed-delivery-persistence" }
makosh-communication-delayed-delivery-runtime = { path = "../communication-delayed-delivery-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
