[package]
name = "makosh-zulip-http"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[features]
default = []
conformance-test-support = []

[package.metadata.makosh]
role = "integration"
owner = "zulip"
surface = "implementation"

[dependencies]
async-native-tls = "=0.6.0"
async-std = "=1.13.2"
makosh-zulip-api = { path = "../zulip-api" }
serde_json = "=1.0.150"
zeroize = "=1.9.0"
