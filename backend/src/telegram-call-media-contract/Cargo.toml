[package]
name = "makosh-telegram-call-media-contract"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "integration"
owner = "telegram"
surface = "contract"

[dependencies]
zeroize = "=1.9.0"
