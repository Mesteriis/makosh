[package]
name = "makosh-communication-explanation-core"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "workflow"
owner = "communication_explanation"
surface = "implementation"

[dependencies]
