[package]
name = "makosh-review-attention-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-review-attention-runtime"
path = "src/main.rs"

[package.metadata.makosh]
role = "domain"
owner = "review"
surface = "runtime"

[dependencies]
makosh-review-attention-api = { path = "../review-attention-api" }
makosh-review-attention-core = { path = "../review-attention-core" }
makosh-review-attention-persistence = { path = "../review-attention-persistence" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
makosh-storage-vault = { path = "../platform/storage/vault" }
libc = "=0.2.186"
prost = "=0.14.4"
sha2 = { version = "=0.11.0", default-features = false }
tokio = { version = "=1.52.4", default-features = false, features = ["rt-multi-thread", "time"] }
zeroize = "=1.9.0"
