[package]
name = "makosh-events-authority"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "events"
surface = "implementation"

[dependencies]
makosh-events-jetstream = { path = "../jetstream" }
zeroize = "=1.9.0"
