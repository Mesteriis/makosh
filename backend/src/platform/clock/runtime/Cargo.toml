[package]
name = "makosh-clock-runtime"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "clock"
surface = "implementation"

[dependencies]
makosh-clock-protocol = { path = "../protocol" }
