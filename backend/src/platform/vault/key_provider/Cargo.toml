[package]
name = "makosh-vault-key-provider"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "vault"
surface = "contract"
