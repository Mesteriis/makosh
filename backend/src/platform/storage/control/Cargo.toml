[package]
name = "makosh-storage-control"
version = "0.1.0"
edition = "2024"
license = "MIT"
[lib]
path = "src/lib.rs"
[package.metadata.makosh]
role = "platform"
owner = "storage"
surface = "implementation"
[dependencies]
makosh-storage-protocol = { path = "../protocol" }
makosh-storage-vault = { path = "../vault" }
