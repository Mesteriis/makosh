[package]
name = "makosh-storage-pgbouncer"
version = "0.1.0"
edition = "2024"
license = "MIT"
[lib]
path = "src/lib.rs"
[package.metadata.makosh]
role = "platform"
owner = "storage"
surface = "implementation"
[dependencies]
makosh-storage-control = { path = "../control" }
makosh-storage-protocol = { path = "../protocol" }
libc = "=0.2.186"
tokio = { version = "=1.52.4", default-features = false, features = ["rt", "time"] }
tokio-postgres = { version = "=0.7.18", default-features = false, features = ["runtime"] }
zeroize = "=1.9.0"
