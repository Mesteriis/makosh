[package]
name = "makosh-blob-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "blob"
surface = "contract"

[dependencies]
