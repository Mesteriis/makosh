[package]
name = "makosh-secure-file"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "secure_file"
surface = "contract"

[dependencies]
libc = "=0.2.186"
