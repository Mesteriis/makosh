[package]
name = "makosh-scheduler-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "scheduler"
surface = "contract"

[dependencies]
makosh-clock-protocol = { path = "../../clock/protocol" }
prost = "=0.14.4"

[build-dependencies]
prost-build = "=0.14.4"
protoc-bin-vendored = "=3.2.0"
