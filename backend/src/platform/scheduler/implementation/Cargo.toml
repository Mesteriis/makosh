[package]
name = "makosh-scheduler"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "platform"
owner = "scheduler"
surface = "implementation"

[dependencies]
makosh-clock-protocol = { path = "../../clock/protocol" }
makosh-events-protocol = { path = "../../events" }
makosh-scheduler-protocol = { path = "../protocol" }
prost = "=0.14.4"
prost-types = "=0.14.4"
