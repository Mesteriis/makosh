[package]
name = "makosh-gateway-protocol"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "api"
owner = "gateway"
surface = "contract"

[dependencies]
makosh-runtime-protocol = { path = "../../../platform/runtime_protocol" }
prost = "=0.14.4"
prost-types = "=0.14.4"

[build-dependencies]
prost-build = "=0.14.4"
protoc-bin-vendored = "=3.2.0"
