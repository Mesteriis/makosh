[package]
name = "makosh-gateway-session-contract"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[package.metadata.makosh]
role = "api"
owner = "gateway"
surface = "contract"
