[package]
name = "makosh-reviewed-task-candidate-promotion-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-reviewed-task-candidate-promotion-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "reviewed_task_candidate_promotion"
surface = "assembly"

[dependencies]
makosh-reviewed-task-candidate-promotion-persistence = { path = "../reviewed-task-candidate-promotion-persistence" }
makosh-reviewed-task-candidate-promotion-runtime = { path = "../reviewed-task-candidate-promotion-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
