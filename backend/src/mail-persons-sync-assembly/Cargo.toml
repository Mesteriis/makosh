[package]
name = "makosh-mail-persons-sync-assembly"
version = "0.1.0"
edition = "2024"
license = "MIT"

[lib]
path = "src/lib.rs"

[[bin]]
name = "makosh-mail-persons-sync-assembly"
path = "src/main.rs"

[package.metadata.makosh]
role = "workflow"
owner = "mail_persons_sync"
surface = "assembly"

[dependencies]
makosh-mail-persons-sync-persistence = { path = "../mail-persons-sync-persistence" }
makosh-mail-persons-sync-runtime = { path = "../mail-persons-sync-runtime" }
makosh-runtime-protocol = { path = "../platform/runtime_protocol" }
makosh-storage-protocol = { path = "../platform/storage/protocol" }
prost = "=0.14.4"
serde = { version = "=1.0.228", default-features = false, features = ["derive", "std"] }
serde_json = "=1.0.150"
