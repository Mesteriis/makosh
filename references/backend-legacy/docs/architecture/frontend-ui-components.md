# Макошь UI Components

## Component inventory

Current foundation patch provides:

```text
ActionBar
ActivityItem
Alert
AlertDialog
AttachmentChip
AttachmentPreview
AudioPlayer
Autocomplete
AsyncSelect
Avatar
Badge
Banner
BottomBar
Breadcrumbs
Button
Card
CardContent
CardDescription
CardFooter
CardHeader
CardTitle
Cascader
CharacterCounter
ChatInput
Checkbox
Chip
CircularProgress
CodeBlock
ColorPicker
ComingSoonState
Command
CommandPalette
ComposerToolbar
Combobox
Container
ContextMenu
CopyButton
Counter
DatePicker
DateTimePicker
DeliveryStatus
DescriptionList
Dialog
Divider
Dock
Drawer
EmptyState
DropdownMenu
DropdownMenuItem
DropdownMenuLabel
DropdownMenuSeparator
EmailInput
EntityIcon
ErrorState
FileDropZone
FileIcon
FilePicker
Flex
FloatingPanel
FocusTrap
Form
FormError
FormField
FormHint
FormLabel
Grid
GroupedSelect
HStack
Heading
HoverCard
HtmlPreview
Icon
IconButton
Image
ImageGallery
ImagePreview
InlineMessage
Input
InspectorPanel
Kbd
KeyboardHint
KeyValue
Label
LinkButton
List
LoadingOverlay
LoadingState
LocaleSwitcher
MarkdownViewer
Mention
Menu
Menubar
MessageBubble
MessageStatus
Metric
MultiSelect
NavigationMenu
NoDataState
NoSearchResultsState
Notification
NumberInput
OfflineState
OTPInput
OverlayHost
Panel
Paper
Paragraph
PasswordInput
Pagination
PDFViewer
Popover
Portal
PresenceIndicator
Progress
ProgressBar
PropertyGrid
ProviderIcon
QuotedMessage
Radio
RadioGroup
RangeSlider
ReactionBadge
ReadReceipt
Resizable
RichTextEditor
ScrollArea
SearchableMultiSelect
SearchableSelect
SearchInput
SearchPalette
Select
Separator
Sheet
Shortcut
SidePanel
Skeleton
Slider
Spinner
Split
Stack
StatusBar
StatusIcon
StatusIndicator
Statistic
Surface
Switch
SyntaxHighlight
TabContent
TabTrigger
Table
Tabs
Tag
Text
TextButton
Textarea
ThemeProvider
ThemeSwitcher
TimePicker
Toast
Toolbar
Tooltip
TopBar
Tree
TreeItem
TreeSelect
TypingIndicator
VStack
TimelineItem
VirtualList
VirtualScrollArea
VirtualTable
VideoPlayer
```

As of the General controls Storybook taxonomy slice, the public barrel exports
178 Макошь UI Vue component entries. The original explicit named component
objective plus later requested additions are represented in code; Storybook
taxonomy is enforced separately so a component cannot count as visually covered
just because it is imported by an aggregate story.
The additional `RichTextEditor` component covers the later compact rich-text authoring
request.
Foundation items are
represented as token/style/documentation surfaces
rather than one Vue component per token. This document is an inventory, not a
claim that the full objective is complete.

## Component ownership

All components live under:

```text
frontend/src/shared/ui
```

Exports live in:

```text
frontend/src/shared/ui/index.ts
```

Styles live in:

```text
frontend/src/shared/ui/styles
```

## Component API policy

Components expose Макошь API, not vendor API.

Good:

```vue
<Button variant="outline" icon="tabler:search">Search</Button>
```

Bad:

```vue
<DropdownMenuRoot>
  <DropdownMenuTrigger />
</DropdownMenuRoot>
```

The second example belongs inside `shared/ui`, never in domains.

## Component styling policy

Components use stable class names:

```text
makosh-btn
makosh-input
makosh-dialog-content
makosh-dropdown-item
```

Class names are intentionally not Tailwind-heavy in templates. Tailwind can still be used in stories or outer layout, but UI Kit components should be restyled through CSS files and tokens.

## Storybook coverage

Storybook is grouped by inspection taxonomy:

```text
Макошь UI
  General
    Button
    Button Group
    Icon Button
    Split Button
    Toggle Group
    Select / Searchable Select / Multi Select / Searchable Multi Select
    Grouped Select / Tree Select / Cascader / Async Select
    Input / Textarea / Search Input / Token Input / Tag Input
    Checkbox / Radio / Switch / Slider
    Date Picker / Date Range Picker / Time Picker
    Menu / Context Menu / Command / Tabs
    Dialog / Drawer / Tooltip / Popover
    Table / List / Tree / Timeline
    Media / Editor / Feedback / Layout / Utility
  Domain
    Communications
  Foundation
    Tokens / Themes / Typography / Icons / Spacing
```

The catalog currently has 53 authored story files and 64 story entries in the
generated Storybook index. `storybookCoverage.boundary.test.ts` enforces the
required General, Domain and Foundation titles and rejects legacy top-level
Storybook groups such as `Макошь UI / Controls`, `Макошь UI / Command` and
`Макошь UI / Media`.

Authored docs live in `Docs.mdx`; autogenerated empty per-story docs are not
enabled for the UI catalog.

The visual baseline matrix covers the generated story index across 3 locales, 3
themes and 7 widths. With the current 64 story entries this is 4032 screenshots.

## Objective completion scope

The explicit named component objective now has no remaining component names:

```text
Communication primitives: implemented.
Utility primitives: implemented.
```

Communication primitives from the objective remain UI-only requirements. They
must not import Telegram, Mail, WhatsApp, provider runtime, domains, stores or
API clients.

`HtmlPreview` is a generic media/viewer primitive for already-prepared HTML or
plain-text body fragments, including email-like content. It must stay provider
neutral, must not import Mail or Communications domains, and must make the
sanitization boundary explicit before rendering HTML.

`RichTextEditor` is a generic authoring primitive for context notes, local drafts
and evidence snippets. It emits sanitized HTML, uses a compact semantic toolbar
instead of document-processor controls, and must stay provider/domain neutral.

## Adding a new component

1. Add component under `frontend/src/shared/ui`.
2. Export from `frontend/src/shared/ui/index.ts`.
3. Add CSS to the smallest matching file under `styles/`.
4. Add Storybook story under `frontend/stories/ui`.
5. Add per-component documentation next to the component.
6. Add a targeted boundary or unit test for the component slice.
7. Run visual snapshot update if Storybook output changed.
8. Do not add domain logic.
9. Do not add API calls.
10. Do not import a domain store.

## Recommended future components

After media, utility and communication slices are completed:

```text
AppMenu
CommandOverlay
ActionList
EntityListItem
ContextCard
InlineStatus
Toolbar
SearchField
KeyboardHint
EmptyState
ErrorState
```

These should remain generic until a domain-specific name is unavoidable.
