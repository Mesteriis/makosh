### Summary / Резюме

В русскую wiki добавляется страница `operations/documentation-map.md`, которая
документирует файл `CONTRIBUTING.md` — его расположение, назначение и ключевые
правила, извлечённые непосредственно из исходного файла. Это первая запись в
карте документации, описывающая руководство для контрибьюторов проекта Макошь.

Другие документы (`AGENTS.md`, ADR) упомянуты в исходном тексте, но не встроены в
данный context pack, поэтому их содержание не может быть подтверждено и описано.

### Proposed pages / Предлагаемые страницы

**`operations/documentation-map.md`**

```markdown
---
title: Карта документации
---

## CONTRIBUTING.md

- **Путь в репозитории:** `CONTRIBUTING.md`
- **Размер:** 1361 байт
- **Назначение:** руководство по участию в разработке Макошь — локальной
  персональной системе памяти для коммуникаций, знаний, отношений, проектов,
  документов, решений, обязательств и контекста.
- **Ключевые разделы:**

### Перед началом работы

- Прочитать `AGENTS.md` и актуальные ADR в `docs/adr/`.
- Не добавлять облачные зависимости, операции записи у провайдеров или
  побочные эффекты агентов ИИ без архитектурного обоснования через ADR.
- Не коммитить секреты, приватные данные почты/сообщений, локальные фикстуры с
  персональным содержимым, `docker/.env` или сгенерированные данные из
  `docker/data/`.
- Изменения должны быть небольшими, тестируемыми и соответствовать структуре
  Rust, SvelteKit и Tauri.

### Настройка окружения для разработки

```sh
make docker-env
make validate
```

Только backend:

```sh
make backend-validate
```

Только frontend:

```sh
make frontend-check
make frontend-build
```

### Pull Request'ы

- Объяснять, что изменено и почему.
- Ссылаться на ADR или документы, ограничивающие изменение.
- Приводить команды валидации и их результаты.
- Добавлять регрессионные тесты при изменении поведения.
- Исключать сгенерированные/локальные данные из диффа.

### Безопасность и приватность

- Импортированные сообщения, документы и данные провайдеров считаются частными
  и недоверенными.
- Не включать приватные пользовательские данные в issues, pull request'ы,
  скриншоты или тестовые фикстуры.
```

### Source coverage / Покрытие источников

| Source file | Covered facts |
|---|---|
| `CONTRIBUTING.md` | Полное содержание: название проекта, его описание (local-first Personal Memory System); условие прочтения `AGENTS.md` и ADR; запрет на облачные зависимости и коммит секретов; ограничения на содержимое коммитов (`docker/.env`, `docker/data/`); требование небольших и тестируемых изменений; команды разработки (`make docker-env`, `make validate`, `make backend-validate`, `make frontend-check`, `make frontend-build`); критерии для pull request (объяснение, ссылки на ADR, результаты валидации, регрессионные тесты, исключение сгенерированных данных); правила безопасности (приватность данных, запрет на включение в issues/PR/screenshots/test fixtures). |

### Drift candidates / Кандидаты на drift

Из предоставленного контекста расхождений (drift) между кодом, документацией и
ADR не видно. В context pack включён только один файл `CONTRIBUTING.md`, поэтому
сравнение с другими артефактами невозможно.
