### Summary / Резюме

Создаётся (или обновляется) страница `operations/documentation-map.md` в русской wiki Макошь. Она централизует ссылки на отчёты по тестовой производительности из директории `reports/test-performance/`, описывает их назначение, фиксирует безопасные точки входа для запуска тестов и архитектурные ограничения, зафиксированные в этих отчётах. Это устраняет ситуацию, когда актуальные измерения и договорённости о тестовом харнессе разбросаны по нескольким файлам без единого навигационного документа.

### Proposed pages / Предлагаемые страницы

#### `operations/documentation-map.md`

```markdown
# Карта документации: операции

_Индексная страница по эксплуатации, тестированию и производительности._

## Отчёты по тестовой производительности

### Базовый замер (baseline)

- [2026-06-23 baseline](../../../reports/test-performance/2026-06-23-baseline.md)
  Объём тестового дерева (файлы, атрибуты `#[test]`, `#[tokio::test]`), медленные backend-наборы с измеренными длительностями, горячие точки компиляции/рантайма, основные узкие места и границы безопасной модернизации.

### Аудит Testcontainers

- [Testcontainers audit](../../../reports/test-performance/2026-06-23-testcontainers-audit.md)
  Проверенное поведение контейнеров PostgreSQL и NATS в `crates/testkit` (общая сессия, изолированные БД, ленивый NATS), риск прямого `cargo test`, изменения в ходе модернизации, оставшиеся возможности оптимизации.

### Сгенерированные отчёты nextest

- [unit-тесты (nextest)](../../../reports/test-performance/unit.md)
  Прогон модульных тестов: **277** тестов, **0** падений, **~200 с**. Самые медленные unit-тесты.
- [backend-full (nextest)](../../../reports/test-performance/backend-full.md)
  Полный прогон backend-тестов: **1401** тест, **0** падений, **1** flaky, **~6710 с**. Самые медленные интеграционные тесты.

### Справочная информация

- [README](../../../reports/test-performance/README.md)
  Описание директории, перечень актуальных отчётов и ссылка на статусную матрицу `docs/development/testing/status.md`.

## Точки входа для запуска тестов

Из базового замера и аудита Testcontainers:

- **Безопасные точки входа**: `make backend-test`, `make backend-validate`, `make test-unit`.
- Полный прогон backend-тестов **обязан** идти через сессионный харнесс `makosh_test_session` (`crates/testkit/src/bin/makosh_test_session.rs`).
- Прямой вызов `cargo test` на всём backend **не допускается** — он обходит контракт общей сессии с контейнерами и может оставлять «мусорные» Docker-контейнеры после прерываний или падений.
- Отчёты nextest генерируются скриптом `scripts/test/analyze-nextest-junit.mjs` (записывает результат в `reports/test-performance/` при явных прогонах).

## Архитектурные заметки

- **PostgreSQL** — основное persistent-хранилище.
- Docker-инфраструктура разработки находится в `docker/` (согласно ADR-0032; содержимое ADR не встроено в этот контекст, достоверность ссылки не подтверждена).
- Полный прогон backend-тестов должен выполняться **исключительно** через харнесс `crates/testkit`.
- **Redis** не вводится в тестовый стек без явного архитектурного решения и реальной потребности конкретной подсистемы Макошь.
```

### Source coverage / Покрытие источников

1. **`reports/test-performance/2026-06-23-baseline.md`**
   - Числовые показатели тестового дерева: 233 файла, 151 top‑level target, 1121 атрибут `#[test]`/`#[tokio::test]`, 263 unit‑test атрибута.
   - Медленные backend-наборы и их длительности (например, `calendar_api.rs` ~196‑197 с).
   - Горячие точки компиляции/рантайма: `ring`, `rustls`, `sqlx-core`, `async-nats`, `testcontainers` и др.
   - Узкие места: тяжёлая интеграционная поверхность, дорогие контейнерные тесты при обходе харнесса, отсутствие автоматической генерации отчётов, недокументированная установка инструментов.
   - Границы безопасной модернизации: PostgreSQL как основное хранилище, Docker‑инфраструктура в `docker/`, харнесс `crates/testkit` обязателен, Redis не вводится искусственно.

2. **`reports/test-performance/2026-06-23-testcontainers-audit.md`**
   - Текущее поведение: контейнеры PostgreSQL и NATS хранятся в `tokio::sync::OnceCell` внутри `crates/testkit/src/context.rs`; `makosh_test_session` создаёт один PostgreSQL‑контейнер на весь прогон и удаляет при выходе; изолированные БД создаются внутри общего контейнера; NATS запускается лениво.
   - Основной риск: прямой полный прогон `cargo test` обходит контракт общей сессии.
   - Изменения в ходе модернизации: точки входа маршрутизированы через `scripts/test/run-nextest.sh` и `run-llvm-cov.sh`; CI‑джобы разделены; руководство предписывает `make backend-test` / `make backend-validate`.
   - Оставшиеся возможности оптимизации (перенос логики в unit‑тесты, слияние повторных bootstrap, группировка nextest).
   - Явный запрет на введение Redis без архитектурного решения.

3. **`reports/test-performance/README.md`**
   - Назначение директории: хранение committed baseline‑замеров и читаемых заметок по оптимизации.
   - Перечень текущих файлов: `2026-06-23-baseline.md`, `2026-06-23-testcontainers-audit.md`, `unit.md`, `unit.json`, `backend-full.md`, `backend-full.json`.
   - Упоминание скрипта‑генератора `scripts/test/analyze-nextest-junit.mjs`.
   - Ссылка на статусную матрицу: `docs/development/testing/status.md`.

4. **`reports/test-performance/backend-full.md`**
   - Общее число тестов: 1401.
   - Общее время прогона: 6710.525 с; среднее 4.79 с; p95 12.795 с; p99 20.413 с.
   - Число упавших тестов: 0; flaky: 1.
   - Перечень 10 самых медленных тестов с указанием полного имени и длительности.

5. **`reports/test-performance/unit.md`**
   - Общее число тестов: 277.
   - Общее время прогона: 200.45 с; среднее 0.724 с; p95 6.467 с; p99 9.608 с.
   - Число упавших тестов: 0; flaky: не обнаружено.
   - Перечень 10 самых медленных unit‑тестов с полными именами.

### Drift candidates / Кандидаты на drift

В предоставленном контексте видны следующие потенциальные расхождения:

1. **Ссылка на `docs/development/testing/status.md`** (из `reports/test-performance/README.md`) — файл не встроен в контекст, его наличие и актуальность не могут быть подтверждены. Если файл отсутствует или устарел, навигация из документационной карты будет нарушена.
2. **Упоминание скриптов `scripts/test/run-nextest.sh`, `run-llvm-cov.sh`, `analyze-nextest-junit.mjs`** — ни один из этих скриптов не предоставлен в source‑pack, поэтому факт их существования и соответствия описанному поведению не верифицируем.
3. **Упоминание ADR‑0032** (из `2026-06-23-baseline.md`) — содержимое ADR отсутствует, проверить актуальность и релевантность решения невозможно.
4. **Упоминание `Makefile`, `crates/testkit`** — сами файлы не встроены; расхождений между отчётами не наблюдается, но актуальное состояние кода может отличаться от зафиксированных в отчётах дат (2026‑06‑23 и 2026‑06‑28).
5. **Наличие файлов `unit.json` и `backend-full.json`** (описаны в README) — не включены в контекст; их отсутствие или отличие от `.md`‑версий может свидетельствовать о незавершённой генерации.

Других расхождений между встроенными исходными файлами не обнаружено.
