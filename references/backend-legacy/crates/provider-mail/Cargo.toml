[package]
name = "makosh-provider-mail"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false
license = "MIT"

[dependencies]
makosh-provider-api = { path = "../provider-api" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
