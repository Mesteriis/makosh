[package]
name = "makosh-provider-telemost"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false
license = "MIT"

[dependencies]
makosh-provider-api = { path = "../provider-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
