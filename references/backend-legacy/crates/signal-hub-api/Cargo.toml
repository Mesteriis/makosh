[package]
name = "makosh-signal-hub-api"
publish = false
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
license = "MIT"

[dependencies]
chrono = "0.4"
makosh-events-api = { path = "../events-api" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
