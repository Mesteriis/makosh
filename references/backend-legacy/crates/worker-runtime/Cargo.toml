[package]
name = "makosh-worker-runtime"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
publish = false
license = "MIT"

[dependencies]
makosh-desktop-runtime = { path = "../desktop-runtime" }
thiserror = "2"
tokio = { version = "1", features = ["rt", "sync"] }
tokio-util = { version = "0.7", features = ["rt"] }
