# Form

Макошь form shell.

Use to group UI-only form fields and emit a controlled submit event.

Do not place API submission, store mutation, or domain validation inside this component.

Accessibility: renders a native form and disabled fieldset wrapper.
