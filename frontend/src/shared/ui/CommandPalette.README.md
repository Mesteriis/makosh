# CommandPalette

Trigger plus Макошь command dialog.

Use for local command discovery and keyboard-first navigation.

Do not execute business mutations directly from this component.

Accessibility: delegates dialog, input and keyboard behavior to `Command`.
