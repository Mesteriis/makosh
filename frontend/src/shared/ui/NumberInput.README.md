# NumberInput

Numeric Макошь input that emits `number | null`.

Use for UI-only numeric controls where typed numeric state is useful.

Do not use for currency, dates, or domain validation without a higher-level component.

Accessibility: provide labels and min/max constraints where applicable.
