# TextButton

Low-emphasis command button for dense Макошь surfaces.

Use for secondary inline actions, toolbar actions, and low-risk commands.

Do not use for navigation or primary destructive confirmation.

Accessibility: it renders a native `button`, supports disabled state, and keeps click emission blocked when disabled.
