# EmailInput

Email-specialized Макошь input.

Use when the browser should expose email keyboard and validation semantics.

Do not perform domain validation or account lookup here.

Accessibility: pair with `FormLabel`; use `ariaLabel` only when a visible label is not available.
