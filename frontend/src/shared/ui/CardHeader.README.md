# CardHeader

Header region for the Макошь `Card` surface.

Use for compact card titles, descriptions, and local actions that identify the
card content.

Do not use as a generic page header or standalone layout wrapper.

Accessibility: keep heading content semantically meaningful and pair actions
with labels that remain clear outside visual context.
