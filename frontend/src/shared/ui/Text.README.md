# Text

Inline or block text primitive for Макошь UI Kit.

Use for short labels, metadata, helper copy, and inline text that needs tokenized size, tone, weight, or truncation.

Do not use for headings, long prose, domain-specific message bodies, or provider-rendered content.

Accessibility: choose semantic `as` values when the text carries structure. Truncation must not be the only place critical information exists.
