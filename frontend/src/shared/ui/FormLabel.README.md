# FormLabel

Label primitive for Макошь form fields.

Use with `for` to connect text to a control.

Do not use as a generic text component; use `Text` or `Label`.

Accessibility: required marker is decorative; validation requirements must be conveyed in help or error text.
